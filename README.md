# 8D Spatial Audio Converter

Convert any audio file to 8D spatial audio with binaural panning.

## Features

- client side processing (no uploads)
- HRTF spatial audio
- live preview & download

## Setup

```bash
git clone <repo-url>
cd spatial-audio
npm install
npm run dev
```
